# Programming 수업 소스 코드
## 02_counter
vanila HTML, CSS, 
Javascript로 만든 Counter
---
## 20260324 React 시작
```shell
npm create vite@latest .
```

### CounterApp
- `useState()`
- `onClick={() => set함수()}`
- `onClick={() => set함수((이전state) => 이전state + 1)}`